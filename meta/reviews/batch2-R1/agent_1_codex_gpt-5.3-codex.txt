`packages/shared/src/ecosystem-id.ts` — **PASS**
- Domain model and naming are coherent (`{prefix}_{opaque}`), prefix registry is explicit, and validation rules are clear for cross-component use.

`packages/shared/src/artifact-ref.ts` — **BLOCKING**
- `parseHepArtifactUri` can throw `URIError` on malformed percent-encoding (`packages/shared/src/artifact-ref.ts:87`, `packages/shared/src/artifact-ref.ts:88`) even though contract says “return `null` if invalid”.  
  Suggested fix: wrap `decodeURIComponent` in `try/catch` and return `null` on decode failure; add test with malformed URI (e.g. `%E0%A4%A`).
- `createArtifactRefV1` does not enforce schema constraints beyond `sha256` (`packages/shared/src/artifact-ref.ts:57`): it currently accepts invalid `size_bytes`/`schema_version`/`created_at` values, which can violate `artifact_ref_v1` contract at runtime.  
  Suggested fix: add full runtime validation (Ajv/Zod or equivalent) for `ArtifactRefV1` fields, especially `size_bytes >= 0`, integer checks, and `created_at` date-time format.

`packages/shared/src/run-state.ts` — **BLOCKING**
- Legacy mapping is incomplete for orchestrator status space: `rejected` exists in orchestrator (`packages/orchestrator/src/types.ts:10`) but has no mapping entry (`packages/shared/src/run-state.ts:72`), so normalization can return `undefined`.  
  Suggested fix: add explicit mapping for `rejected` (likely to `failed`, if that is the intended canonical collapse) and add a test.

`packages/shared/src/gate-registry.ts` — **ADVISORY**
- Cross-component naming boundary is still ambiguous: shared registry uses names like `approval_run_start` (`packages/shared/src/gate-registry.ts:45`) while orchestrator still uses `A1..A5`.  
  Suggested improvement: add an explicit alias map or conversion helpers to avoid drift/misalignment during integration.
- Comment says “compile-time uniqueness check” but current check is runtime (`packages/shared/src/gate-registry.ts:100`).

**Overall verdict: `NOT_CONVERGED`** (blocking issues present).