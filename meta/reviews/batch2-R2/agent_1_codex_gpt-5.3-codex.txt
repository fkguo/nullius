`packages/shared/src/artifact-ref.ts` — **ADVISORY**  
- `parseHepArtifactUri` fix is correct (`decodeURIComponent` guarded; malformed encoding now returns `null`) at `packages/shared/src/artifact-ref.ts:86`.  
- Non-blocking contract clarity concern: `isHepArtifactUri` is regex-only (`packages/shared/src/artifact-ref.ts:78`) and can still return `true` for malformed percent-encoding that `parseHepArtifactUri` rejects.  
- Suggested refinement: either make `isHepArtifactUri` decode-aware, or explicitly document it as syntactic-only validation.

`packages/shared/src/run-state.ts` — **PASS**  
- Blocking issue resolved: `rejected: 'failed'` added in legacy map at `packages/shared/src/run-state.ts:92`.  
- Semantics and naming are consistent with canonical terminal state model.

`packages/shared/src/gate-registry.ts` — **PASS**  
- Advisory comment wording fixed correctly (`module-load uniqueness check`) at `packages/shared/src/gate-registry.ts:100`.  
- No contract or semantics regressions.

Verification run: `pnpm -C packages/shared test` passed (77/77).

Overall verdict: **CONVERGED** (0 blocking).