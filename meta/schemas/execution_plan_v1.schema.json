{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://autoresearch.dev/schemas/execution_plan_v1.schema.json",
  "title": "ExecutionPlan V1",
  "description": "Provider-neutral audited IR for bridging staged idea surfaces into a materialized computation manifest. Captures provenance, task-level capability needs, and expected artifacts without fixing provider routing as long-term authority.",
  "type": "object",
  "required": ["schema_version", "run_id", "objective", "source", "tasks"],
  "properties": {
    "schema_version": { "type": "integer", "const": 1, "description": "Schema version, always 1 for this schema." },
    "run_id": { "type": "string", "minLength": 1, "description": "Run identifier this execution plan belongs to." },
    "objective": { "type": "string", "minLength": 1, "description": "Human-readable objective derived from the staged thesis." },
    "source": {
      "type": "object",
      "required": ["outline_seed_path", "source_handoff_uri"],
      "properties": {
        "outline_seed_path": { "type": "string", "minLength": 1, "description": "Canonical run-local path to outline_seed_v1.json." },
        "source_handoff_uri": { "type": "string", "minLength": 1, "description": "Original source handoff URI preserved from the staged idea surface." },
        "campaign_id": { "type": "string", "description": "Optional campaign provenance copied from IdeaHandoffC2 when available." },
        "node_id": { "type": "string", "description": "Optional node provenance copied from IdeaHandoffC2 when available." },
        "idea_id": { "type": "string", "description": "Optional idea provenance copied from IdeaHandoffC2 when available." },
        "promoted_at": { "type": "string", "format": "date-time", "description": "Optional promotion timestamp copied from IdeaHandoffC2 when available." },
        "required_observables": {
          "type": "array",
          "items": { "type": "string", "minLength": 1 },
          "description": "Optional observable-level hints copied from the staged handoff."
        },
        "candidate_formalisms": {
          "type": "array",
          "items": { "type": "string", "minLength": 1 },
          "description": "Optional non-authoritative method/formalism hints preserved for local context."
        },
        "method_spec_present": {
          "type": "boolean",
          "description": "Whether an upstream method_spec-like payload was present on the staged handoff surface."
        },
        "method_hint_count": { "type": "integer", "minimum": 0, "description": "Number of structured method hints consumed by the compiler." }
      },
      "additionalProperties": false
    },
    "tasks": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": ["task_id", "title", "hypothesis_indices", "claim_indices", "method_hint_indices", "capabilities", "expected_artifacts"],
        "properties": {
          "task_id": { "type": "string", "minLength": 1 },
          "title": { "type": "string", "minLength": 1 },
          "description": { "type": "string" },
          "hypothesis_indices": {
            "type": "array",
            "items": { "type": "integer", "minimum": 0 },
            "description": "Indices into outline_seed_v1.hypotheses that motivate this task."
          },
          "claim_indices": {
            "type": "array",
            "items": { "type": "integer", "minimum": 0 },
            "description": "Indices into outline_seed_v1.claims that inform this task."
          },
          "method_hint_indices": {
            "type": "array",
            "items": { "type": "integer", "minimum": 0 },
            "description": "Indices into staged method hints consumed by this task."
          },
          "observables": { "type": "array", "items": { "type": "string", "minLength": 1 } },
          "method_hint_summary": { "type": "string", "description": "Non-authoritative summary of the method hint that informed this task." },
          "capabilities": {
            "type": "array",
            "minItems": 1,
            "items": { "type": "string", "minLength": 1 },
            "description": "Provider-neutral capability requirements for this task."
          },
          "depends_on_task_ids": {
            "type": "array",
            "items": { "type": "string", "minLength": 1 },
            "description": "Optional future-facing DAG dependencies."
          },
          "expected_artifacts": {
            "type": "array",
            "minItems": 1,
            "items": {
              "type": "object",
              "required": ["artifact_id", "kind", "path"],
              "properties": {
                "artifact_id": { "type": "string", "minLength": 1 },
                "kind": { "type": "string", "minLength": 1 },
                "path": { "type": "string", "minLength": 1 },
                "description": { "type": "string" }
              },
              "additionalProperties": false
            }
          },
          "lowering_hints": {
            "type": "object",
            "properties": {
              "workspace_subdir": { "type": "string", "minLength": 1 }
            },
            "additionalProperties": false,
            "description": "Optional generic hints reserved for the manifest materializer."
          }
        },
        "additionalProperties": false
      }
    },
    "created_at": { "type": "string", "format": "date-time", "description": "ISO 8601 UTC timestamp when this plan was compiled." }
  },
  "additionalProperties": false
}
