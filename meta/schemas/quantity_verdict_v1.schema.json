{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://nullius.dev/schemas/quantity_verdict_v1.schema.json",
  "title": "Quantity Verdict V1",
  "description": "Domain-neutral, closed verdict artifact binding one verification outcome to an explicit non-empty set of quantity identifiers. Display and other acceptance gates consume this artifact only after validating its schema identity and version.",
  "type": "object",
  "required": [
    "schema_id",
    "schema_version",
    "quantities",
    "verdict"
  ],
  "properties": {
    "schema_id": {
      "type": "string",
      "const": "quantity_verdict_v1"
    },
    "schema_version": {
      "type": "integer",
      "const": 1
    },
    "quantities": {
      "type": "array",
      "minItems": 1,
      "uniqueItems": true,
      "items": {
        "$ref": "#/$defs/QuantityVerdictIdentifier"
      },
      "description": "Stable identifiers of every quantity covered by this verdict."
    },
    "verdict": {
      "$ref": "#/$defs/QuantityVerdictOutcome"
    }
  },
  "additionalProperties": false,
  "$defs": {
    "QuantityVerdictIdentifier": {
      "title": "Quantity Verdict Identifier",
      "type": "string",
      "minLength": 1,
      "pattern": ".*\\S.*"
    },
    "QuantityVerdictOutcome": {
      "title": "Quantity Verdict Outcome",
      "description": "Closed verification outcome. A consuming gate decides which outcome is acceptable; producers cannot extend this vocabulary.",
      "type": "string",
      "enum": [
        "pass",
        "fail"
      ]
    }
  }
}
