// Keep these assertions line-stable: the root checker and doc drift tests use
// exact substring matches so front-door wording drift fails closed.
export const FRONT_DOOR_SNIPPETS = [
  {
    relPath: 'docs/QUICKSTART.md',
    snippets: [
      '本页面向当前最成熟的 domain pack 上手路径，而不是重新定义 root 产品身份。generic lifecycle + workflow-plan front door 仍是 `nullius`；这里的 `hep_*` 路径只是在此基础上进入当前最强的 HEP evidence/project/run workflow family。',
      '## Generic First-Touch（先走 generic front door）',
	      '1) `nullius init --project-root /absolute/path/to/external-project`',
	      '2) `nullius status --project-root /absolute/path/to/external-project`',
	      '建立 `.nullius/HARNESS` runtime handshake 与 `.nullius/` lifecycle/control-plane 状态。',
	      '`nullius workflow-plan --recipe literature_to_evidence`',
      '这是一个公开的 stateful front door，会直接通过 `@nullius/literature-workflows` 解析 checked-in workflow authority，并写入 `.nullius/state.json#/plan` / `.nullius/plan.md`。',
      '重要文献 note 必须全文/source-first 阅读，记录 section/page/equation/figure 覆盖，并用 LaTeX math 写科学记号',
      '原始检索摘要、下载尝试和控制面观察写入 `research_plan.md` progress log 或 `artifacts/runs/<run_id>/`',
      '不要把它们当成新的 quickstart 默认入口。',
    ],
    orderedSnippets: [
      '## Generic First-Touch（先走 generic front door）',
      '1) `nullius init --project-root /absolute/path/to/external-project`',
      '2) `nullius status --project-root /absolute/path/to/external-project`',
      '3) `nullius workflow-plan --recipe literature_to_evidence`',
      '## Draft Path（最简路径）',
      '1) `hep_project_create`',
    ],
    forbiddenSnippets: [
      '`python3 skills/research-team/scripts/bin/literature_fetch.py workflow-plan` 仍是较底层的并行 consumer。',
    ],
  },
  {
    relPath: 'README.md',
    snippets: [
      'Nullius is a domain-neutral, evidence-first research monorepo.',
      '- `nullius` remains the stateful CLI front door for initialized external project roots. Use it for lifecycle state, bounded execution, `workflow-plan`, verification, higher-conclusion gating, and proposal decisions.',
      '- `orch_*` remains the MCP/operator counterpart of that same control plane. It is a host-facing bridge for the control plane, not a competing product identity and not a replacement for the CLI.',
      '- `openalex_*`, `arxiv_*`, `hepdata_*`, `pdg_*`, and `zotero_*` remain bounded atomic MCP operators. They stay MCP-first because they are schema-driven provider atoms, not stateful workflow shells that need mass CLI mirroring.',
      'The idea-engine search/eval runtime is archived; contracts + store are retained, scoring consumes an external belief-graph posterior (pinned tool, current pin gaia-lang==0.5.0a4), and idea-engine is still not a default capability-expansion lane.',
      'posterior-based rank/promote and node posterior/lifecycle updates remain `idea-engine` runtime-contract truth, not a root front door',
      '| Stateful CLI front door | `nullius` | External project-root lifecycle state, approvals, bounded native TS `run --workflow-id computation`, stateful `workflow-plan` persistence, and `graph` dependency-map rendering (claims / progress / literature / roadmap) |',
      '| Control-plane MCP/operator counterpart | `orch_*` | Host-facing MCP/operator surface for the same lifecycle/control-plane authority |',
      '| Stateful literature planning | `nullius workflow-plan` | Checked-in workflow authority resolved via `@nullius/literature-workflows`, persisted to `.nullius/state.json#/plan`, and rendered to `.nullius/plan.md` |',
      '| Provider atoms | `openalex_*`, `arxiv_*`, `hepdata_*`, `pdg_*`, `zotero_*` | Bounded, schema-driven MCP operators are easier to compose than provider-local CLI mirrors |',
      '`research_plan.md#Current Status` is the human status entry',
      'Literature notes for important sources must be full-text/source-first with auditable section/page/equation/figure coverage and LaTeX math notation.',
      'Keep dated run logs, raw workflow summaries, and tool-use traces in `research_plan.md` progress entries or `artifacts/runs/<run_id>/`',
      'Paper originals, extracted text, arXiv source tarballs, and source trees are ordinary local files.',
      'Research material placement is filesystem-first: transient fetches stay in local temp, while material needed for verification or continuation becomes a project/run artifact.',
      'MCP environment paths are filesystem roots, not URI/protocol settings.',
	      'If `nullius` is not on `PATH`, create a local wrapper after building:',
	      'This repository is currently a local workspace, not a published global npm CLI.',
	      '- For stateful literature workflows, first initialize the target external project root with `nullius init`, then use `nullius workflow-plan` from that root or with `--project-root`. It resolves recipes directly via `@nullius/literature-workflows`, persists `.nullius/state.json#/plan`, and derives `.nullius/plan.md`.',
	      'You are in a folder that should be managed by nullius.',
	      'If AGENTS.md and .nullius/HARNESS are both missing, initialize the project:',
	      'If `nullius` is not available, first prepare the CLI from the source checkout',
	      'Once initialized, reconnect is local-first:',
	      'If you want the generic lifecycle/control-plane smoke path first:',
      '1. `nullius init --project-root /absolute/path/to/external-project`',
      '1. `nullius status --project-root /absolute/path/to/external-project`',
      '- the root product identity',
    ],
    orderedSnippets: [
      '## 1. Surface Policy',
      '- `nullius` remains the stateful CLI front door for initialized external project roots. Use it for lifecycle state, bounded execution, `workflow-plan`, verification, higher-conclusion gating, and proposal decisions.',
      '- `orch_*` remains the MCP/operator counterpart of that same control plane. It is a host-facing bridge for the control plane, not a competing product identity and not a replacement for the CLI.',
      '## 2. Current Public Surfaces',
      '| Stateful CLI front door | `nullius` | External project-root lifecycle state, approvals, bounded native TS `run --workflow-id computation`, stateful `workflow-plan` persistence, and `graph` dependency-map rendering (claims / progress / literature / roadmap) |',
      '| Control-plane MCP/operator counterpart | `orch_*` | Host-facing MCP/operator surface for the same lifecycle/control-plane authority |',
      '| Stateful literature planning | `nullius workflow-plan` | Checked-in workflow authority resolved via `@nullius/literature-workflows`, persisted to `.nullius/state.json#/plan`, and rendered to `.nullius/plan.md` |',
      '| Current most mature domain MCP front door | `node /absolute/path/to/nullius/packages/hep-mcp/dist/index.js` | HEP domain MCP server for research, evidence, writing, export, and provider-local composition |',
      '## 3. Layer Model',
      'If you want the generic lifecycle/control-plane smoke path first:',
      'If you want the current strongest domain-pack smoke path next, connect your MCP client to `packages/hep-mcp/dist/index.js` and run:',
    ],
    forbiddenSnippets: [
      '`literature-gap` is still live only as a legacy shell',
      '`literature-gap` still exists in the legacy Pipeline A CLI surface',
      '`literature-gap` remains only as a legacy wrapper',
    ],
  },
  {
    relPath: 'docs/README_zh.md',
    snippets: [
      'Nullius 是一个面向理论研究的 domain-neutral、evidence-first monorepo。',
      '- `nullius` 继续作为已初始化外部 project root 的 stateful CLI front door。lifecycle state、bounded execution、`workflow-plan`、verification、更高结论 gate，以及 proposal decisions 都从这里进入。',
      '- `orch_*` 继续作为同一 control plane 的 MCP/operator counterpart。它是 control plane 的 host-facing bridge，不是另一个产品身份，也不是对 CLI 的替代。',
      '- `openalex_*`、`arxiv_*`、`hepdata_*`、`pdg_*`、`zotero_*` 继续作为 bounded atomic MCP operators。它们保持 MCP-first，因为这些 surface 是 schema-driven provider atoms，而不是需要整套 CLI 镜像的 stateful workflow shell。',
      'idea-engine 的 search/eval runtime 已归档；contracts + store 保留，打分消费外部 belief-graph posterior（pinned tool，当前 pin gaia-lang==0.5.0a4），idea-engine 仍不是默认 capability expansion lane。',
      'posterior 排序的 rank/promote 与节点 posterior/lifecycle 更新属于 `idea-engine` runtime-contract truth，不是 root front door',
      '| Stateful CLI front door | `nullius` | 外部 project-root lifecycle state、审批、受限原生 TS `run --workflow-id computation`、stateful `workflow-plan` 持久化，以及 `graph` 依赖图渲染（claims / progress / literature / roadmap） |',
      '| Control-plane MCP/operator counterpart | `orch_*` | 面向 host 的 MCP/operator surface，承载同一套 lifecycle/control-plane authority |',
      '| Stateful 文献规划入口 | `nullius workflow-plan` | 通过 `@nullius/literature-workflows` 解析 checked-in workflow authority，并写入 `.nullius/state.json#/plan` / `.nullius/plan.md` |',
      '| Provider atoms | `openalex_*`、`arxiv_*`、`hepdata_*`、`pdg_*`、`zotero_*` | bounded、schema-driven MCP operators 比 provider-local CLI mirrors 更易组合 |',
	      '`research_plan.md#Current Status` 是给人看的状态入口',
      '重要文献 note 必须全文/source-first 阅读，记录可审查的 section/page/equation/figure 覆盖，并用 LaTeX math 写科学记号。',
      '带日期的执行记录、原始 workflow 摘要和工具调用过程应放在 `research_plan.md` progress log 或 `artifacts/runs/<run_id>/`',
      '论文原文、抽取文本、arXiv source tarball 和源码树都是普通本机文件。',
      '研究材料放置遵循 filesystem-first：临时抓取放本机临时目录，需要后续验证或接续的材料写成 project/run artifact。',
      'MCP env 里的路径是 filesystem roots，不是 URI/protocol 设置。',
	      '如果 `nullius` 不在 `PATH` 中，构建后创建本地 wrapper：',
	      '当前仓库是本地 workspace，不是已发布的全局 npm CLI。',
	      '- 对 stateful 文献工作流，先用 `nullius init` 初始化目标外部 project root，再在该 root 内或通过 `--project-root` 调用 `nullius workflow-plan`。它会直接通过 `@nullius/literature-workflows` 解析 recipe，并写入 `.nullius/state.json#/plan` / `.nullius/plan.md`。',
	      '`.nullius/HARNESS` 是 `nullius init` 写入的机器可读 runtime handshake',
	      'You are in a folder that should be managed by nullius.',
	      'If AGENTS.md and .nullius/HARNESS are both missing, initialize the project:',
	      'If `nullius` is not available, first prepare the CLI from the source checkout',
	      '初始化完成后，接续是 local-first 的',
	      '如果你想先走 generic lifecycle/control-plane 烟测路径：',
      '1. `nullius init --project-root /absolute/path/to/external-project`',
      '1. `nullius status --project-root /absolute/path/to/external-project`',
      '- root 产品身份本身',
    ],
    orderedSnippets: [
      '## 1. Surface Policy',
      '- `nullius` 继续作为已初始化外部 project root 的 stateful CLI front door。lifecycle state、bounded execution、`workflow-plan`、verification、更高结论 gate，以及 proposal decisions 都从这里进入。',
      '- `orch_*` 继续作为同一 control plane 的 MCP/operator counterpart。它是 control plane 的 host-facing bridge，不是另一个产品身份，也不是对 CLI 的替代。',
      '## 2. 当前公开 Surface',
      '| Stateful CLI front door | `nullius` | 外部 project-root lifecycle state、审批、受限原生 TS `run --workflow-id computation`、stateful `workflow-plan` 持久化，以及 `graph` 依赖图渲染（claims / progress / literature / roadmap） |',
      '| Control-plane MCP/operator counterpart | `orch_*` | 面向 host 的 MCP/operator surface，承载同一套 lifecycle/control-plane authority |',
      '| Stateful 文献规划入口 | `nullius workflow-plan` | 通过 `@nullius/literature-workflows` 解析 checked-in workflow authority，并写入 `.nullius/state.json#/plan` / `.nullius/plan.md` |',
      '| 当前最成熟的领域 MCP front door | `node /absolute/path/to/nullius/packages/hep-mcp/dist/index.js` | 面向研究、证据、写作、导出与 provider-local 组合的 HEP 领域 MCP server |',
      '## 3. Layer Model',
      '如果你想先走 generic lifecycle/control-plane 烟测路径：',
      '如果你接着想走当前最强的 domain-pack 烟测路径，再把 MCP client 接到 `packages/hep-mcp/dist/index.js` 并执行：',
    ],
    forbiddenSnippets: [
      '`literature-gap` 仍然存在，但只作为 legacy shell',
      '`literature-gap` 仍在旧的 Pipeline A CLI 面上存活',
      '`literature-gap` 仅剩 legacy wrapper',
    ],
  },
  {
    relPath: 'docs/PROJECT_STATUS.md',
    snippets: [
      '**Root framing**: Domain-neutral substrate + control plane; HEP is the current most mature provider family, not the root identity',
	      '**Main generic lifecycle + native TS computation + workflow-plan entrypoint**: `nullius` CLI for external project roots and `.nullius/` state',
	      '**Runtime project handshake**: `nullius init` writes `.nullius/HARNESS`',
	      '- Closeout baseline: `main` at `ecd9faa8`',
      '- **Recommended public stateful literature workflow entrypoint**: `nullius workflow-plan` (requires an initialized external project root; resolves recipes directly via `@nullius/literature-workflows`; persists `.nullius/state.json#/plan` and derives `.nullius/plan.md`)',
      '- **Native TS run slice**: `nullius run` (requires an initialized external project root; runs prepared `computation/manifest.json` natively for `--workflow-id computation`, and also advances dependency-satisfied persisted workflow-plan steps through the same front door)',
      'the idea-engine search/eval runtime is archived; `@nullius/idea-mcp` remains campaign-lifecycle oriented on explicit external data roots',
      'posterior-based ranking and promotion inside the runtime contract, not as a root front door or a new default capability lane',
      '- **Public stateful literature planning workflow**: `nullius workflow-plan` resolves literature recipes directly via `@nullius/literature-workflows` into bounded executable steps for an initialized external project root, persists the plan substrate into `.nullius/state.json#/plan`, and derives `.nullius/plan.md`',
      '**Native TS run workflow**: `nullius run` remains the only execution front door; `--workflow-id computation` executes a prepared `computation/manifest.json`, while persisted workflow-plan steps advance in a bounded loop until completion or a blocking failure is reached',
      '**Generic lifecycle workflow**: `nullius init/status/approve/pause/resume/export`',
      '**Project/Run evidence workflow**: `hep_project_create` -> `hep_run_create` -> evidence build/query -> `hep_render_latex` -> `hep_export_project`',
      '- `workflow-plan` 现在是公开的 stateful literature front door，且已把稳定的 typed `plan.execution` metadata 写入 `.nullius/state.json#/plan`。',
      '- `nullius run` 现在是该 seam 的 canonical bounded consumer：它会推进 dependency-satisfied persisted workflow steps，直到完成或遇到 blocking failure，并继续保持唯一 execution front door。',
      '- 当前 slice 仍未提供 canonical closed-loop literature execution runtime；这里还没有 full scheduler、多步自主编排或 end-to-end closed loop。',
      'important literature notes require full-text/source-first reading, auditable section/page/equation/figure coverage, and LaTeX math notation',
      'dated execution traces, raw run observations, and tool-use traces belong in `research_plan.md` or `artifacts/runs/<run_id>/`',
      'Paper originals, extracted text, arXiv source tarballs, and source trees are filesystem materials',
    ],
    orderedSnippets: [
      '- **Main generic lifecycle + native TS computation + workflow-plan entrypoint**: `nullius` CLI for external project roots and `.nullius/` state',
      '- **Recommended public stateful literature workflow entrypoint**: `nullius workflow-plan` (requires an initialized external project root; resolves recipes directly via `@nullius/literature-workflows`; persists `.nullius/state.json#/plan` and derives `.nullius/plan.md`)',
      '- **Native TS run slice**: `nullius run` (requires an initialized external project root; runs prepared `computation/manifest.json` natively for `--workflow-id computation`, and also advances dependency-satisfied persisted workflow-plan steps through the same front door)',
      '- **Current most mature domain MCP front door**: `@nullius/hep-mcp` exposed through `packages/hep-mcp/dist/index.js`',
      '- **Generic lifecycle workflow**: `nullius init/status/approve/pause/resume/export`',
      '- **Public stateful literature planning workflow**: `nullius workflow-plan` resolves literature recipes directly via `@nullius/literature-workflows` into bounded executable steps for an initialized external project root, persists the plan substrate into `.nullius/state.json#/plan`, and derives `.nullius/plan.md`',
      '- **Native TS run workflow**: `nullius run` remains the only execution front door; `--workflow-id computation` executes a prepared `computation/manifest.json`, while persisted workflow-plan steps advance in a bounded loop until completion or a blocking failure is reached',
      '- **Project/Run evidence workflow**: `hep_project_create` -> `hep_run_create` -> evidence build/query -> `hep_render_latex` -> `hep_export_project`',
      '## Workflow-plan boundary',
      '- `workflow-plan` 现在是公开的 stateful literature front door，且已把稳定的 typed `plan.execution` metadata 写入 `.nullius/state.json#/plan`。',
    ],
    forbiddenSnippets: [
      '`literature-gap` remains legacy-only',
      '`literature-gap` is still live on the legacy Pipeline A CLI surface',
    ],
  },
  {
    relPath: 'docs/ARCHITECTURE.md',
    snippets: [
      '## 1. Stable Layer Model',
      '| Workflow authority | checked-in recipes consumed by `nullius workflow-plan` | High-level workflow meaning stays above provider packs |',
      '| Stateful control plane | `nullius` plus `orch_*` | One shared authority for lifecycle state, approvals, bounded execution, verification, proposal decisions, and read models |',
      '## 2. Design Invariants',
	      '- `nullius` remains the stateful CLI front door. `orch_*` remains the MCP/operator counterpart of that same control plane.',
	      '- `nullius init` writes `.nullius/HARNESS` as the machine-readable project handshake.',
	      '- Provider MCP surfaces stay MCP-first. We do not mass-convert provider MCP into CLI because those surfaces are bounded atoms, not stateful workflow shells.',
      '## 3. Current Public Surfaces',
      '| Stateful CLI front door | `nullius` | Generic lifecycle state, `workflow-plan`, bounded native TS computation, verification, higher-conclusion gating, proposal decisions, and `graph` dependency-map rendering (consumes the domain-neutral `@nullius/shared/graph-viz` engine: claims / progress / literature / roadmap) |',
      '| Control-plane MCP/operator surface | `orch_*` | Canonical public MCP/operator counterpart of the same control plane |',
      'the idea-engine search/eval runtime is archived rather than a default capability-expansion lane',
      'posterior-based `rank.compute` / `node.promote` stay inside the `idea-engine` runtime contract',
      'important literature notes require full-text/source-first reading, auditable section/page/equation/figure coverage, and LaTeX math notation',
      'raw dated run logs and tool-use traces belong in `research_plan.md` or `artifacts/runs/<run_id>/`',
      '### 5.3 Research material placement',
      'Paper originals, extracted text, arXiv source tarballs, and source trees are filesystem materials, not product-level protocol surfaces.',
      '## 4. Why Provider MCP Stays MCP',
      '- Stateful workflow entry remains above provider packs: `nullius workflow-plan` owns checked-in workflow authority, and `nullius run` remains the only execution front door.',
      'Users who need generic lifecycle state should invoke `nullius` directly rather than expecting the root MCP server to own that surface today.',
    ],
    forbiddenSnippets: [
      '`literature-gap` still exists on the legacy Pipeline A CLI surface as a wrapper',
    ],
  },
  {
    relPath: 'docs/TOOL_CATEGORIES.md',
    snippets: [
      '由公开的 stateful front door 解析后，再下沉到 `inspire_search` / provenance / network operators',
      '不再通过 provider-specific high-level MCP facade',
      '高层 literature workflow 现由公开的 stateful `nullius workflow-plan` 前门承载，需先 `nullius init` 并且会直接通过 `@nullius/literature-workflows` 解析后写入 `.nullius/state.json#/plan` / `.nullius/plan.md`。',
    ],
    forbiddenSnippets: [
      '`literature-gap` 仅剩 legacy shell',
      '`literature-gap` 不再作为推荐主入口',
      '`literature-gap` 仍是 legacy shell',
    ],
  },
  {
    relPath: 'docs/TESTING_GUIDE.md',
    snippets: [
      '本指南面向手工验收当前 front-door truth。`nullius` 是 generic lifecycle + workflow-plan front door；`@nullius/hep-mcp` 是当前最成熟的 domain MCP front door，提供 Project/Run、evidence、writing/export、literature/data、Zotero、PDG 能力。本页重点覆盖两者的衔接，而不是把 `hep-mcp` 重新写成 root 产品身份。',
      '本文所有 MCP 配置都以 `packages/hep-mcp/dist/index.js` 为当前 domain MCP front door，而不是 generic root front door。',
      '### 0.0 先确认 front-door 角色',
	      '- `nullius` = generic lifecycle + workflow-plan front door',
	      '- `.nullius/HARNESS` = `nullius init` 写入的机器可读 runtime handshake',
	      '- `@nullius/hep-mcp` = 当前最成熟的 domain MCP front door',
      '- legacy Python CLI 不再属于公开 front-door；如仍需覆盖，只作为 maintainer/eval/regression-only 内部路径测试',
      '重要文献 note 必须记录全文/source-first 阅读、section/page/equation/figure 覆盖，并用 LaTeX math 写科学记号',
      '### 5.4 stateful literature workflow consumers',
      '这部分不是 MCP 工具，而是当前真实存在的高层 workflow consumers：',
      '这个推荐的公开 stateful front door 会直接通过 `@nullius/literature-workflows` 解析 checked-in workflow authority，并写入 `.nullius/state.json#/plan` / `.nullius/plan.md`。',
      'Maintainer / eval / regression only:',
      '旧的 provider-local Python parser 已删除；maintainer/eval/regression proof 现在改由 lower-level checked-in coverage 提供：',
      '`pnpm --filter @nullius/literature-workflows test -- tests/resolve.test.ts`',
      '`pnpm --filter @nullius/orchestrator test -- tests/nullius-cli.test.ts`',
      '- `nullius workflow-plan` 仍是唯一 installable public high-level literature entrypoint',
      '- lower-level checked-in runner / resolver / front-door coverage 仍能证明 `literature_gap_analysis` recipe、seed-search 解析、analyze-step wiring 与 live CLI truth',
    ],
    forbiddenSnippets: [
      '`literature-gap` 仅剩 legacy shell',
    ],
  },
  {
    relPath: 'meta/protocols/session_protocol_v1.md',
    snippets: [
      '> This protocol is a checked-in workflow authority artifact for Stage 1-2 entry guidance and is executed through the package-local workflow recipes in `@nullius/literature-workflows`, with `nullius workflow-plan` as the installable public stateful front door. Other checked-in consumers remain internal-only validation seams of the same authority, not competing entrypoints.',
      '> The old provider-local literature-gap shell path is deleted. High-level literature entry stays on checked-in workflow recipes plus `nullius workflow-plan`.',
    ],
    forbiddenSnippets: [
      '`research-team` and provider-local literature-gap.',
    ],
  },
  {
    relPath: 'meta/docs/orchestrator-mcp-tools-spec.md',
    snippets: [
      '**Rule**: `orch_*` owns lifecycle state, approvals, queueing, and orchestration policy.',
      '5. `nullius` remains the generic front door for lifecycle / workflow-plan / bounded computation; `orch_*` is the MCP/operator counterpart of that control plane rather than a competing product identity.',
      '`hep://` and `orch://` are intentionally separate owned namespaces. Cross-scheme correlation must be carried explicitly by workflow metadata or operator context, not by implicit aliasing.',
      '2. The provider-local Python parser package has been retired. Do not recreate provider-local Python control-plane authority.',
    ],
  },
  {
    relPath: 'docs/URI_REGISTRY.md',
    snippets: [
      'The current emitted/resolved URI schemes covered by this registry are `hep://`, `pdg://`, `orch://`, `rep://`, `hepdata://`, `openalex://`, `zotero://`, and `file://`.',
      'It is not an MCP resources contract; current MCP servers expose tools only.',
      '`hep://` and `orch://` are separate owned namespaces.',
      'There is no implicit `hep://` <-> `orch://` aliasing layer in live authority.',
      '| `hep://` | `@nullius/hep-mcp` | `packages/hep-mcp/src/core/runArtifactUri.ts`; `packages/hep-mcp/src/core/uriReader.ts` | Tool-return/local artifact identifiers, not MCP resources | `hep://projects`; `hep://runs`; `hep://projects/{project_id}`; `hep://projects/{project_id}/papers`; `hep://projects/{project_id}/artifact/{artifact_name}`; `hep://projects/{project_id}/papers/{paper_id}`; `hep://projects/{project_id}/papers/{paper_id}/evidence/catalog`; `hep://runs/{run_id}/manifest`; `hep://runs/{run_id}/artifact/{artifact_name}` | HEP project/run manifests and research artifacts only. It does not own orchestrator lifecycle/read-model state. |',
      '| `pdg://` | `@nullius/pdg-mcp` | `packages/pdg-mcp/src/artifacts.ts`; `packages/pdg-mcp/src/tools/registry.ts` | Tool-return/local artifact identifiers, not MCP resources | `pdg://artifacts/{artifact_name}` | PDG local artifact cache identifiers only. Full artifacts live under `PDG_DATA_DIR/artifacts`; the server does not expose MCP resources. |',
      '| `orch://` | `@nullius/orchestrator` | `packages/orchestrator/src/orch-tools/{approval,control,create-status-list,run-read-model}.ts`; `packages/orchestrator/src/workflow-runtime.ts`; `packages/orchestrator/src/cli-run.ts` | Tool-return lifecycle/read-model/artifact identifiers | `orch://runs/{run_id}`; `orch://runs/{run_id}/approvals/{approval_dir}`; `orch://runs/{run_id}/artifact/{artifact_path}`; `orch://runs/export` | Orchestrator lifecycle/read-model/export summaries and orchestrator-owned workflow/result artifacts only. It does not own HEP research artifact payloads. |',
      '| `zotero://` | `@nullius/zotero-mcp` | `packages/zotero-mcp/src/zotero/tools.ts` | Tool-return Zotero select URI, not MCP resources | `zotero://select/library/items/{item_key}` | Local Zotero item selection/deep-link pointer only. `@nullius/zotero-mcp` does not advertise MCP resources. |',
    ],
    forbiddenSnippets: [
      '`hep://corpora` | Live',
      '| `orch://runs/{run_id}/state` | Live',
      '| `orch://runs/{run_id}/ledger` | Live',
    ],
  },
];

export const REQUIRED_PACKAGE_DESCRIPTION_SNIPPETS = [
  'Nullius ecosystem monorepo',
  'control-plane',
  'provider packages',
];

export const FORBIDDEN_EXACT_PACKAGE_NAMES = new Set(['agent', 'nullius-agent']);
export const RETIRED_EXACT_PACKAGE_NAMES = new Set([[ 'hep', 'nullius' ].join('-')]);
export const FORBIDDEN_PACKAGE_TOKENS = new Set(['shell', 'gateway', 'frontend']);
