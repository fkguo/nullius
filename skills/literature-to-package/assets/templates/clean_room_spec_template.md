# Clean-room SPEC: <method name>

Written FROM THE LITERATURE ONLY. This document must not quote, paraphrase, or
name any reference implementation; every statement cites an extraction item.

## Inputs / outputs
- Inputs: <types, domains, units — cite extraction items>
- Outputs: <types, ranges, units — cite extraction items>

## Definitions and conventions
<every symbol, normalization, sign convention, unit system — cite extraction items by id>

## Algorithm
<numbered steps precise enough to implement without seeing any code>

## Edge cases and validity domain
<limits, singular points, preconditions under which the method is valid>

## Acceptance
<which reference-check rows this method must reproduce, with tolerances>
