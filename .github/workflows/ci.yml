name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [20, 22]
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: pnpm

      - name: Install uv
        run: python3 -m pip install uv

      - run: pnpm install --frozen-lockfile

      - run: pnpm -r build

      - run: pnpm --filter @autoresearch/hep-mcp docs:tool-counts:check

      - run: pnpm -r lint

      - run: pnpm -r test

      - run: make contract-test

  python-test:
    runs-on: ubuntu-latest
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Python tests (hep-autoresearch)
        run: |
          cd packages/hep-autoresearch
          if [ -f pyproject.toml ] || [ -f setup.py ]; then
            pip install -e ../project-contracts
            pip install -e ".[dev]" 2>/dev/null || pip install -e .
            pip install pytest
            if [ -d tests ] && find tests -name 'test_*.py' | head -1 | grep -q .; then
              python -m pytest tests/ -q
            else
              echo "No test files found, skipping"
            fi
          else
            echo "No Python package found, skipping"
          fi

      - name: Python tests (idea-core)
        run: |
          cd packages/idea-core
          if [ -f pyproject.toml ] || [ -f setup.py ]; then
            pip install -e ".[dev]" 2>/dev/null || pip install -e .
            pip install pytest
            if [ -d tests ] && find tests -name 'test_*.py' | head -1 | grep -q .; then
              python -m pytest tests/ -q
            else
              echo "No test files found, skipping"
            fi
          else
            echo "No Python package found, skipping"
          fi
