# hep-research-mcp tool inventory

- Generated at (UTC): 2026-02-01T15:09:40.158712+00:00
- Repo root: `/home/user/Seafile/AI/hep-research-mcp-main`
- Package dir: `/home/user/Seafile/AI/hep-research-mcp-main/packages/hep-research-mcp`
- Entrypoint: `dist/index.js`
- Version: `0.3.0`
- Git commit: `f147de5ff1a2ce0dab57321d4e528f53136ae0de`
- Git dirty: `False`

## standard (72)

### hep (45)

- `hep_export_paper_scaffold` — Export a publication-ready paper/ scaffold for a run (RevTeX4-2): materialize LaTeX + bib split into paper/ and write a portable paper_sc...
- `hep_export_project` — Export run outputs as research assets: master.bib, report.tex, report.md, research_pack.zip, and NotebookLM-friendly artifacts (Evidence-...
- `hep_health` — Server health/config snapshot + optional INSPIRE connectivity probe (local-only; network optional).
- `hep_import_from_zotero` — Import Zotero items into a run mapping (Zotero item → identifiers → INSPIRE recid). Requires Zotero Local API; resolves via INSPIRE when ...
- `hep_import_paper_bundle` — Import a finalized paper/ (post research-writer) back into a run as portable artifacts (paper_bundle.zip + paper_bundle_manifest.json + o...
- `hep_inspire_resolve_identifiers` — Batch resolve identifiers (recid/arXiv/DOI) to INSPIRE recids and write mapping artifacts (network; Evidence-first URIs + summary).
- `hep_inspire_search_export` — Export an INSPIRE search (safe pagination) to run artifacts (jsonl/json). Network (INSPIRE) + Evidence-first output (URIs + summary).
- `hep_project_build_evidence` — Build a LaTeX Evidence Catalog for a project paper and write `catalog.jsonl` (Evidence-first: URI + summary; local-only)
- `hep_project_create` — Create a local project (Project/Run foundation) and return `hep://projects/{project_id}` (read details via MCP resources; local-only)
- `hep_project_get` — Get a project summary + `hep://projects/{project_id}` URI (read the full manifest via MCP resources; local-only)
- `hep_project_list` — List local projects (small summaries + `hep://projects/{project_id}` URIs; read details via MCP resources; local-only)
- `hep_project_playback_evidence` — Playback an evidence locator and return a stable snippet (local-only)
- `hep_project_query_evidence` — Lexical query over a project Evidence Catalog (returns top hits + `evidence_id`/locator; no embeddings; local-only)
- `hep_project_query_evidence_semantic` — Semantic query interface for a project Evidence Catalog. Currently falls back to lexical search and writes a run artifact (URI + summary;...
- `hep_render_latex` — Render structured SectionDraft/ReportDraft into LaTeX, enforce allowed_citations via citation verifier, and write artifacts (Evidence-fir...
- `hep_run_build_citation_mapping` — Build bibliography→INSPIRE mapping artifacts for a run (runs locally; uses INSPIRE network) and write `bibliography_raw.json`, `citekey_t...
- `hep_run_build_evidence_index_v1` — Build LaTeX EvidenceChunks + BM25 index for a run (fail-fast; no PDF fallback) and write artifacts (Evidence-first)
- `hep_run_build_measurements` — Extract HEP-style numeric measurements from a run LaTeX evidence catalog and write artifacts + diagnostics (Evidence-first, local-only).
- `hep_run_build_pdf_evidence` — Build PDF evidence for a run (text pages + optional visual region snippets) and write artifacts (Evidence-first, local-only)
- `hep_run_build_writing_critical` — Build writing-critical artifacts for a run: conflicts.json, stance.jsonl, evidence_grades.json, and a summary (Evidence-first, local-only)
- `hep_run_build_writing_evidence` — Build reusable writing evidence artifacts for a run (LaTeX evidence catalog + embeddings + enrichment; optional PDF evidence; Evidence-fi...
- `hep_run_clear_manifest_lock` — Clear a stale run manifest lock file (fail-fast; local-only). Use force=true only if you are sure no other tool is updating the run manif...
- `hep_run_create` — Create a run under a project (writes run manifest + artifacts dir; returns `hep://runs/{run_id}` URIs; local-only)
- `hep_run_read_artifact_chunk` — Read a small byte-range chunk from a HEP run artifact (debug helper; returns base64 chunk; local-only). Note: This tool only works for HE...
- `hep_run_stage_content` — Stage large client content into a run artifact and return its URI (token-limit safe two-step submission; local-only)
- `hep_run_writing_build_evidence_packet_section_v2` — Build per-section retrieval candidates, run LLM rerank (client/internal), and write `writing_evidence_packet_section_###_v2.json` (fail-f...
- `hep_run_writing_create_outline_candidates_packet_v1` — Create an N-best outline candidates prompt_packet + next_actions (M13; N>=2 hard requirement; Evidence-first; fail-fast; local-only).
- `hep_run_writing_create_outline_judge_packet_v1` — Create a Judge prompt_packet for selecting the best outline candidate (M13; hard gates; Evidence-first; fail-fast; local-only).
- `hep_run_writing_create_paperset_curation_packet` — Create a PaperSetCuration prompt_packet artifact for client paperset planning (Evidence-first; writes run artifact; local-only).
- `hep_run_writing_create_revision_plan_packet_v1` — Create a RevisionPlanV1 prompt_packet for client revision planning (requires ReviewerReport v2; Evidence-first; local-only).
- `hep_run_writing_create_section_candidates_packet_v1` — Create an N-best section candidates prompt_packet + next_actions (M13; N>=2 hard requirement; Evidence-first; fail-fast; local-only).
- `hep_run_writing_create_section_judge_packet_v1` — Create a Judge prompt_packet for selecting the best section candidate (M13; hard gates; Evidence-first; fail-fast; local-only).
- `hep_run_writing_create_section_write_packet_v1` — Create a per-section PromptPacket + evidence context artifacts for client section writing, gated by TokenGate (M06; fail-fast; Evidence-f...
- `hep_run_writing_create_token_budget_plan_v1` — Create a `writing_token_budget_plan_v1.json` artifact for a run (M05: TokenBudgetPlan SSOT; overflow_policy=fail_fast; Evidence-first; lo...
- `hep_run_writing_integrate_sections_v1` — Integrate run writing_section_*.json into writing_integrated.tex + diagnostics and run LaTeX compile gate (fail-fast; Evidence-first; loc...
- `hep_run_writing_refinement_orchestrator_v1` — Advance one writing refinement round state machine step (Review → RevisionPlan → Execute → Re-verify → Integrate → Repeat) (Evidence-firs...
- `hep_run_writing_submit_outline_candidates_v1` — Submit N-best outline candidates (strict validation; writes writing_candidates_outline_v1.json; fail-fast; local-only).
- `hep_run_writing_submit_outline_judge_decision_v1` — Submit a client-generated JudgeDecision for outline selection, enforce hard gates, then write writing_outline_v2.json (M13; fail-fast; lo...
- `hep_run_writing_submit_paperset_curation` — Submit a client-generated PaperSetCuration into run artifacts (fail-fast validated; writes writing_paperset_v1.json; local-only).
- `hep_run_writing_submit_rerank_result_v1` — Submit client LLM rerank indices (for a previously generated rerank packet) and write `writing_rerank_result_section_###_v1.json` + `writ...
- `hep_run_writing_submit_review` — Submit a client-generated reviewer report for a completed run write (Evidence-first: writes run artifacts; local-only).
- `hep_run_writing_submit_revision_plan_v1` — Submit a client-generated RevisionPlan v1 into run artifacts (fail-fast validated; writes writing_revision_plan_round_XX_v1.json; local-o...
- `hep_run_writing_submit_section_candidates_v1` — Submit N-best section candidates (strict schema validation; writes writing_candidates_section_###_v1.json; fail-fast; local-only).
- `hep_run_writing_submit_section_judge_decision_v1` — Submit a client-generated JudgeDecision for section selection, enforce hard gates, then run verifiers (M13; fail-fast; local-only).
- `hep_run_writing_token_gate_v1` — Run TokenGate on a prompt_packet (+ optional evidence_packet) and write either token gate pass or writing_token_overflow artifacts (M05; ...

### inspire (12)

- `inspire_advanced` — Unified advanced INSPIRE tools (network). Modes: parse_latex_content/find_experts/analyze_papers/find_connections/trace_original_source
- `inspire_critical_research` — Unified critical research tool (network). Modes: evidence/conflicts/analysis/reviews/theoretical
- `inspire_deep_research` — End-to-end deep research pipeline over a paper set. Modes: analyze/synthesize/write. write mode is Evidence-first via `run_id` (writes ar...
- `inspire_discover_papers` — Unified paper discovery tool (network). Modes: seminal/related/expansion/survey
- `inspire_field_survey` — Physicist's literature review workflow (network): reviews → seminal → citation expansion → controversies
- `inspire_literature` — Unified INSPIRE literature access tool (network). Modes + required args: - get_paper: { recid } - get_references: { recid, size? } - look...
- `inspire_network_analysis` — Unified network analysis tool (network). Modes: citation/collaboration
- `inspire_paper_source` — Unified paper source access tool (network). Modes: urls/content/metadata/auto (downloads arXiv sources/PDFs and optionally extracts). - '...
- `inspire_search` — Search INSPIRE-HEP literature database (network). Supports combining multiple conditions in one query. Note: Some MCP clients prefix tool...
- `inspire_search_next` — Follow an INSPIRE `next_url` returned by `inspire_search` with strict same-origin checks (network; avoids arbitrary URL fetch).
- `inspire_style_corpus_query` — Query a journal style corpus (default: RMP) for style evidence (local-only). Evidence-first: - Writes a compact query result artifact to ...
- `inspire_topic_analysis` — Unified topic analysis tool (network). Modes: timeline/evolution/emerging/all

### pdg (8)

- `pdg_find_particle` — Find particle candidates by name / MCID / PDG identifier (small result; supports pagination; local-only; requires `PDG_DB_PATH`).
- `pdg_find_reference` — Find PDG references by DOI / INSPIRE recid / document id / title (small result; supports pagination; local-only; requires `PDG_DB_PATH`).
- `pdg_get` — Get a PDG identifier object (writes a JSON artifact; returns URI + summary; local-only; requires `PDG_DB_PATH`).
- `pdg_get_decays` — List decay modes for a particle (writes JSONL artifact; returns URI + summary; local-only; requires `PDG_DB_PATH`).
- `pdg_get_measurements` — List PDG measurements for an identifier (writes JSONL artifact; includes references/values/footnotes; local-only; requires `PDG_DB_PATH`)...
- `pdg_get_property` — Get a high-frequency particle property (mass/width/lifetime) with uncertainties and PDG locator (local-only; requires `PDG_DB_PATH`).
- `pdg_get_reference` — Get a PDG reference record (small result; includes INSPIRE lookup identifiers; local-only; requires `PDG_DB_PATH`).
- `pdg_info` — Return PDG MCP server info and local data directories (small result; local-only) (requires `PDG_DB_PATH`).

### zotero (7)

- `zotero_add` — Preview a Zotero add/update operation and return a confirm_token; execute via zotero_confirm (local-only write). Sources: `item` (no netw...
- `zotero_confirm` — Confirm and execute a previewed Zotero write operation (local-only). Use the confirm_token returned by tools like zotero_add.
- `zotero_export_items` — Export Zotero items into BibTeX/CSL-JSON/RIS/etc via Local API (local-only).
- `zotero_find_items` — Resolve Zotero items by identifiers (doi/arxiv/recid/item_key/title) with optional local filters (tags/authors/publication_title/year/vol...
- `zotero_get_selected_collection` — Resolve the Zotero UI-selected collection to a Local API collection_key (requires Zotero Connector + Zotero open; maps connector path → L...
- `zotero_local` — Unified Zotero Local API tool (requires Zotero Local API at `http://127.0.0.1:23119`). Modes: list_collections/list_collection_paths/list...
- `zotero_search_items` — Browse/search Zotero items via Zotero Local API query params (q/qmode/tag/itemType, optional collection scope). Returns summarized items ...

## full (84)

### hep (45)

- `hep_export_paper_scaffold` — Export a publication-ready paper/ scaffold for a run (RevTeX4-2): materialize LaTeX + bib split into paper/ and write a portable paper_sc...
- `hep_export_project` — Export run outputs as research assets: master.bib, report.tex, report.md, research_pack.zip, and NotebookLM-friendly artifacts (Evidence-...
- `hep_health` — Server health/config snapshot + optional INSPIRE connectivity probe (local-only; network optional).
- `hep_import_from_zotero` — Import Zotero items into a run mapping (Zotero item → identifiers → INSPIRE recid). Requires Zotero Local API; resolves via INSPIRE when ...
- `hep_import_paper_bundle` — Import a finalized paper/ (post research-writer) back into a run as portable artifacts (paper_bundle.zip + paper_bundle_manifest.json + o...
- `hep_inspire_resolve_identifiers` — Batch resolve identifiers (recid/arXiv/DOI) to INSPIRE recids and write mapping artifacts (network; Evidence-first URIs + summary).
- `hep_inspire_search_export` — Export an INSPIRE search (safe pagination) to run artifacts (jsonl/json). Network (INSPIRE) + Evidence-first output (URIs + summary).
- `hep_project_build_evidence` — Build a LaTeX Evidence Catalog for a project paper and write `catalog.jsonl` (Evidence-first: URI + summary; local-only)
- `hep_project_create` — Create a local project (Project/Run foundation) and return `hep://projects/{project_id}` (read details via MCP resources; local-only)
- `hep_project_get` — Get a project summary + `hep://projects/{project_id}` URI (read the full manifest via MCP resources; local-only)
- `hep_project_list` — List local projects (small summaries + `hep://projects/{project_id}` URIs; read details via MCP resources; local-only)
- `hep_project_playback_evidence` — Playback an evidence locator and return a stable snippet (local-only)
- `hep_project_query_evidence` — Lexical query over a project Evidence Catalog (returns top hits + `evidence_id`/locator; no embeddings; local-only)
- `hep_project_query_evidence_semantic` — Semantic query interface for a project Evidence Catalog. Currently falls back to lexical search and writes a run artifact (URI + summary;...
- `hep_render_latex` — Render structured SectionDraft/ReportDraft into LaTeX, enforce allowed_citations via citation verifier, and write artifacts (Evidence-fir...
- `hep_run_build_citation_mapping` — Build bibliography→INSPIRE mapping artifacts for a run (runs locally; uses INSPIRE network) and write `bibliography_raw.json`, `citekey_t...
- `hep_run_build_evidence_index_v1` — Build LaTeX EvidenceChunks + BM25 index for a run (fail-fast; no PDF fallback) and write artifacts (Evidence-first)
- `hep_run_build_measurements` — Extract HEP-style numeric measurements from a run LaTeX evidence catalog and write artifacts + diagnostics (Evidence-first, local-only).
- `hep_run_build_pdf_evidence` — Build PDF evidence for a run (text pages + optional visual region snippets) and write artifacts (Evidence-first, local-only)
- `hep_run_build_writing_critical` — Build writing-critical artifacts for a run: conflicts.json, stance.jsonl, evidence_grades.json, and a summary (Evidence-first, local-only)
- `hep_run_build_writing_evidence` — Build reusable writing evidence artifacts for a run (LaTeX evidence catalog + embeddings + enrichment; optional PDF evidence; Evidence-fi...
- `hep_run_clear_manifest_lock` — Clear a stale run manifest lock file (fail-fast; local-only). Use force=true only if you are sure no other tool is updating the run manif...
- `hep_run_create` — Create a run under a project (writes run manifest + artifacts dir; returns `hep://runs/{run_id}` URIs; local-only)
- `hep_run_read_artifact_chunk` — Read a small byte-range chunk from a HEP run artifact (debug helper; returns base64 chunk; local-only). Note: This tool only works for HE...
- `hep_run_stage_content` — Stage large client content into a run artifact and return its URI (token-limit safe two-step submission; local-only)
- `hep_run_writing_build_evidence_packet_section_v2` — Build per-section retrieval candidates, run LLM rerank (client/internal), and write `writing_evidence_packet_section_###_v2.json` (fail-f...
- `hep_run_writing_create_outline_candidates_packet_v1` — Create an N-best outline candidates prompt_packet + next_actions (M13; N>=2 hard requirement; Evidence-first; fail-fast; local-only).
- `hep_run_writing_create_outline_judge_packet_v1` — Create a Judge prompt_packet for selecting the best outline candidate (M13; hard gates; Evidence-first; fail-fast; local-only).
- `hep_run_writing_create_paperset_curation_packet` — Create a PaperSetCuration prompt_packet artifact for client paperset planning (Evidence-first; writes run artifact; local-only).
- `hep_run_writing_create_revision_plan_packet_v1` — Create a RevisionPlanV1 prompt_packet for client revision planning (requires ReviewerReport v2; Evidence-first; local-only).
- `hep_run_writing_create_section_candidates_packet_v1` — Create an N-best section candidates prompt_packet + next_actions (M13; N>=2 hard requirement; Evidence-first; fail-fast; local-only).
- `hep_run_writing_create_section_judge_packet_v1` — Create a Judge prompt_packet for selecting the best section candidate (M13; hard gates; Evidence-first; fail-fast; local-only).
- `hep_run_writing_create_section_write_packet_v1` — Create a per-section PromptPacket + evidence context artifacts for client section writing, gated by TokenGate (M06; fail-fast; Evidence-f...
- `hep_run_writing_create_token_budget_plan_v1` — Create a `writing_token_budget_plan_v1.json` artifact for a run (M05: TokenBudgetPlan SSOT; overflow_policy=fail_fast; Evidence-first; lo...
- `hep_run_writing_integrate_sections_v1` — Integrate run writing_section_*.json into writing_integrated.tex + diagnostics and run LaTeX compile gate (fail-fast; Evidence-first; loc...
- `hep_run_writing_refinement_orchestrator_v1` — Advance one writing refinement round state machine step (Review → RevisionPlan → Execute → Re-verify → Integrate → Repeat) (Evidence-firs...
- `hep_run_writing_submit_outline_candidates_v1` — Submit N-best outline candidates (strict validation; writes writing_candidates_outline_v1.json; fail-fast; local-only).
- `hep_run_writing_submit_outline_judge_decision_v1` — Submit a client-generated JudgeDecision for outline selection, enforce hard gates, then write writing_outline_v2.json (M13; fail-fast; lo...
- `hep_run_writing_submit_paperset_curation` — Submit a client-generated PaperSetCuration into run artifacts (fail-fast validated; writes writing_paperset_v1.json; local-only).
- `hep_run_writing_submit_rerank_result_v1` — Submit client LLM rerank indices (for a previously generated rerank packet) and write `writing_rerank_result_section_###_v1.json` + `writ...
- `hep_run_writing_submit_review` — Submit a client-generated reviewer report for a completed run write (Evidence-first: writes run artifacts; local-only).
- `hep_run_writing_submit_revision_plan_v1` — Submit a client-generated RevisionPlan v1 into run artifacts (fail-fast validated; writes writing_revision_plan_round_XX_v1.json; local-o...
- `hep_run_writing_submit_section_candidates_v1` — Submit N-best section candidates (strict schema validation; writes writing_candidates_section_###_v1.json; fail-fast; local-only).
- `hep_run_writing_submit_section_judge_decision_v1` — Submit a client-generated JudgeDecision for section selection, enforce hard gates, then run verifiers (M13; fail-fast; local-only).
- `hep_run_writing_token_gate_v1` — Run TokenGate on a prompt_packet (+ optional evidence_packet) and write either token gate pass or writing_token_overflow artifacts (M05; ...

### inspire (23)

- `inspire_advanced` — Unified advanced INSPIRE tools (network). Modes: parse_latex_content/find_experts/analyze_papers/find_connections/trace_original_source
- `inspire_analyze_citation_stance` — [Advanced] Analyze how a paper cites another paper (stance detection; resolves citekeys via INSPIRE as needed; network).
- `inspire_cleanup_downloads` — [Advanced] Delete cached arXiv source download directories created by this server (marker-checked). Supports `dry_run`, `older_than_hours...
- `inspire_critical_research` — Unified critical research tool (network). Modes: evidence/conflicts/analysis/reviews/theoretical
- `inspire_deep_research` — End-to-end deep research pipeline over a paper set. Modes: analyze/synthesize/write. write mode is Evidence-first via `run_id` (writes ar...
- `inspire_discover_papers` — Unified paper discovery tool (network). Modes: seminal/related/expansion/survey
- `inspire_field_survey` — Physicist's literature review workflow (network): reviews → seminal → citation expansion → controversies
- `inspire_find_crossover_topics` — [Advanced] Discover emerging interdisciplinary research areas by analyzing papers spanning multiple arXiv categories (network).
- `inspire_literature` — Unified INSPIRE literature access tool (network). Modes + required args: - get_paper: { recid } - get_references: { recid, size? } - look...
- `inspire_network_analysis` — Unified network analysis tool (network). Modes: citation/collaboration
- `inspire_paper_source` — Unified paper source access tool (network). Modes: urls/content/metadata/auto (downloads arXiv sources/PDFs and optionally extracts). - '...
- `inspire_search` — Search INSPIRE-HEP literature database (network). Supports combining multiple conditions in one query. Note: Some MCP clients prefix tool...
- `inspire_search_next` — Follow an INSPIRE `next_url` returned by `inspire_search` with strict same-origin checks (network; avoids arbitrary URL fetch).
- `inspire_style_corpus_build_evidence` — [Advanced] Build Docling-like LaTeX evidence catalogs for a style corpus (local compute; optional INSPIRE citation mapping when enabled; ...
- `inspire_style_corpus_build_index` — [Advanced] Build hybrid retrieval index for a style corpus (local-only)
- `inspire_style_corpus_build_manifest` — [Advanced] Build/extend a style corpus manifest from INSPIRE search results (incremental + deterministic; network).
- `inspire_style_corpus_download` — [Advanced] Download style corpus papers from arXiv (LaTeX preferred, PDF fallback; checkpointable; network).
- `inspire_style_corpus_export_pack` — [Advanced] Export a style corpus pack (zip + sha256 manifest) for transfer to another machine (local-only)
- `inspire_style_corpus_import_pack` — [Advanced] Import a style corpus pack (zip + sha256 manifest) into HEP_DATA_DIR/corpora (local-only)
- `inspire_style_corpus_init_profile` — [Advanced] Initialize a built-in style corpus profile on disk (local-only; Evidence-first)
- `inspire_style_corpus_query` — Query a journal style corpus (default: RMP) for style evidence (local-only). Evidence-first: - Writes a compact query result artifact to ...
- `inspire_topic_analysis` — Unified topic analysis tool (network). Modes: timeline/evolution/emerging/all
- `inspire_validate_bibliography` — [Advanced] Cross-validate LaTeX bibliography entries against INSPIRE for a given paper identifier (recid/arXiv/DOI) and report matches/di...

### pdg (9)

- `pdg_batch` — Execute multiple PDG tool calls in one request (writes a JSON artifact; supports limited parallelism; local-only; requires `PDG_DB_PATH` ...
- `pdg_find_particle` — Find particle candidates by name / MCID / PDG identifier (small result; supports pagination; local-only; requires `PDG_DB_PATH`).
- `pdg_find_reference` — Find PDG references by DOI / INSPIRE recid / document id / title (small result; supports pagination; local-only; requires `PDG_DB_PATH`).
- `pdg_get` — Get a PDG identifier object (writes a JSON artifact; returns URI + summary; local-only; requires `PDG_DB_PATH`).
- `pdg_get_decays` — List decay modes for a particle (writes JSONL artifact; returns URI + summary; local-only; requires `PDG_DB_PATH`).
- `pdg_get_measurements` — List PDG measurements for an identifier (writes JSONL artifact; includes references/values/footnotes; local-only; requires `PDG_DB_PATH`)...
- `pdg_get_property` — Get a high-frequency particle property (mass/width/lifetime) with uncertainties and PDG locator (local-only; requires `PDG_DB_PATH`).
- `pdg_get_reference` — Get a PDG reference record (small result; includes INSPIRE lookup identifiers; local-only; requires `PDG_DB_PATH`).
- `pdg_info` — Return PDG MCP server info and local data directories (small result; local-only) (requires `PDG_DB_PATH`).

### zotero (7)

- `zotero_add` — Preview a Zotero add/update operation and return a confirm_token; execute via zotero_confirm (local-only write). Sources: `item` (no netw...
- `zotero_confirm` — Confirm and execute a previewed Zotero write operation (local-only). Use the confirm_token returned by tools like zotero_add.
- `zotero_export_items` — Export Zotero items into BibTeX/CSL-JSON/RIS/etc via Local API (local-only).
- `zotero_find_items` — Resolve Zotero items by identifiers (doi/arxiv/recid/item_key/title) with optional local filters (tags/authors/publication_title/year/vol...
- `zotero_get_selected_collection` — Resolve the Zotero UI-selected collection to a Local API collection_key (requires Zotero Connector + Zotero open; maps connector path → L...
- `zotero_local` — Unified Zotero Local API tool (requires Zotero Local API at `http://127.0.0.1:23119`). Modes: list_collections/list_collection_paths/list...
- `zotero_search_items` — Browse/search Zotero items via Zotero Local API query params (q/qmode/tag/itemType, optional collection scope). Returns summarized items ...

## full - standard

- Extra tools: 12
  - `inspire_analyze_citation_stance`
  - `inspire_cleanup_downloads`
  - `inspire_find_crossover_topics`
  - `inspire_style_corpus_build_evidence`
  - `inspire_style_corpus_build_index`
  - `inspire_style_corpus_build_manifest`
  - `inspire_style_corpus_download`
  - `inspire_style_corpus_export_pack`
  - `inspire_style_corpus_import_pack`
  - `inspire_style_corpus_init_profile`
  - `inspire_validate_bibliography`
  - `pdg_batch`
