# Revision eval fixture project

This directory is a minimal deterministic fixture for the `revision` workflow.

- It is intentionally scoped to evals.
- It is not a user-facing project template.
- The checked-in revision input is `research_contract.md`, not a legacy notebook surface.
- The package repo root should not depend on a package-root `paper/` or package-root machine contract anymore.
