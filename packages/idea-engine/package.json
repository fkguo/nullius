{
  "name": "@autoresearch/idea-engine",
  "version": "0.0.1",
  "private": true,
  "description": "TS idea campaign runtime and contract authority",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "fixtures:regen:check": "node tests/fixtures/regenerate-rpc-goldens.mjs --check",
    "fixtures:regen:write": "node tests/fixtures/regenerate-rpc-goldens.mjs --write",
    "lint": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "ajv": "^8.18.0",
    "canonicalize": "^2.1.0"
  },
  "devDependencies": {
    "@types/node": "^25.0.3",
    "typescript": "^5.4.0",
    "vitest": "^4.0.18"
  }
}
