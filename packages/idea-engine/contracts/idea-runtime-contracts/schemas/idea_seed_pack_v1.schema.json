{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "idea_seed_pack_v1.schema.json",
  "title": "IdeaSeedPack v1",
  "description": "SSOT artifact: campaign-scoped wrapper around SeedPack (normalized seeds + provenance).",
  "$comment": "Stub schema (v0.2): seed provenance may expand (e.g., C1 gap refs, PDG tension refs, KB note refs).",
  "type": "object",
  "required": ["campaign_id", "seed_pack", "created_at"],
  "properties": {
    "campaign_id": { "type": "string", "format": "uuid" },
    "seed_pack": { "$ref": "./seed_pack_v1.schema.json" },
    "created_at": { "type": "string", "format": "date-time" },
    "extensions": { "type": "object", "additionalProperties": true }
  },
  "additionalProperties": false
}
