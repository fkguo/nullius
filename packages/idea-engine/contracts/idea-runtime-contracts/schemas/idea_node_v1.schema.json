{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "idea_node_v1.schema.json",
  "title": "IdeaNode v1",
  "description": "A single candidate idea node in a campaign portfolio, with lineage, traces, diagnostics, an optional formalized IdeaCard, and an externally computed belief posterior.\n\nMutability contract (audit-first): Fields campaign_id, idea_id, node_id, parent_node_ids, island_id, operator_id, operator_family, origin, operator_trace, rationale_draft, created_at are IMMUTABLE after creation. Fields revision, idea_card, grounding_audit, reduction_report, reduction_audit, posterior, lifecycle_state, activation_condition, updated_at are MUTABLE (append/replace semantics; prior values should be preserved in the ledger or history artifacts). revision is engine-managed: initialized to 1 at creation and incremented on each mutation.",
  "type": "object",
  "required": [
    "campaign_id",
    "idea_id",
    "node_id",
    "revision",
    "parent_node_ids",
    "operator_id",
    "origin",
    "operator_trace",
    "rationale_draft",
    "created_at"
  ],
  "properties": {
    "campaign_id": {
      "type": "string",
      "pattern": "^[0123456789abcdefghjkmnpqrstvwxyz]{8}$"
    },
    "idea_id": {
      "type": "string",
      "pattern": "^[0123456789abcdefghjkmnpqrstvwxyz]{8}$"
    },
    "node_id": {
      "type": "string",
      "pattern": "^[0123456789abcdefghjkmnpqrstvwxyz]{8}$"
    },
    "revision": {
      "type": "integer",
      "minimum": 1,
      "description": "Monotonically increasing revision counter. Incremented on each mutation (posterior update, lifecycle change, grounding_audit, idea_card update). Enables stale-read detection and future optimistic concurrency."
    },
    "parent_node_ids": {
      "type": "array",
      "items": {
        "type": "string",
        "pattern": "^[0123456789abcdefghjkmnpqrstvwxyz]{8}$"
      }
    },
    "island_id": {
      "type": "string",
      "minLength": 1,
      "description": "Historical origin/lineage tag recorded at creation (population partition of earlier campaign runs). Immutable; not interpreted by the current runtime. New nodes may omit it."
    },
    "operator_id": {
      "type": "string",
      "minLength": 1,
      "description": "Origin/lineage tag: identifier of the procedure that created this node (e.g., seed.import). Immutable."
    },
    "operator_family": {
      "type": "string",
      "minLength": 1,
      "description": "Origin/lineage tag: family of the creating procedure (stable tag for audits). Immutable; recommended but not required."
    },
    "rationale_draft": {
      "$ref": "./rationale_draft_v1.schema.json"
    },
    "idea_card": {
      "oneOf": [
        {
          "$ref": "./idea_card_v1.schema.json"
        },
        {
          "type": "null"
        }
      ]
    },
    "origin": {
      "type": "object",
      "required": [
        "model",
        "temperature",
        "prompt_hash",
        "timestamp",
        "role"
      ],
      "properties": {
        "model": {
          "type": "string",
          "minLength": 1
        },
        "temperature": {
          "type": "number",
          "minimum": 0
        },
        "prompt_hash": {
          "type": "string",
          "pattern": "^sha256:[a-f0-9]{64}$"
        },
        "timestamp": {
          "type": "string",
          "format": "date-time"
        },
        "role": {
          "type": "string",
          "minLength": 1,
          "description": "Agent role/persona that produced this node (e.g., Ideator, Librarian, Formalizer, Checker)."
        }
      },
      "additionalProperties": false
    },
    "operator_trace": {
      "type": "object",
      "required": [
        "inputs",
        "params",
        "evidence_uris_used"
      ],
      "properties": {
        "inputs": {
          "type": "object"
        },
        "params": {
          "type": "object"
        },
        "random_seed": {
          "type": "integer"
        },
        "evidence_uris_used": {
          "type": "array",
          "items": {
            "type": "string",
            "format": "uri"
          }
        },
        "prompt_snapshot_hash": {
          "type": "string",
          "pattern": "^sha256:[a-f0-9]{64}$"
        }
      },
      "additionalProperties": false
    },
    "lifecycle_state": {
      "enum": [
        "active",
        "waiting_activation",
        "archived"
      ],
      "description": "Portfolio lifecycle state. Absent means active (validator-side default). waiting_activation nodes carry an activation_condition and are excluded from ranking; archived nodes are retained as an idea archive and are excluded from ranking and promotion."
    },
    "posterior": {
      "oneOf": [
        {
          "type": "null"
        },
        {
          "type": "object",
          "required": [
            "value",
            "evidence_count",
            "updated_at"
          ],
          "properties": {
            "value": {
              "type": "number",
              "minimum": 0,
              "maximum": 1,
              "description": "Posterior belief in the idea's significance, computed by an external belief-graph tool from source-grounded sub-criteria."
            },
            "evidence_count": {
              "type": "integer",
              "minimum": 0,
              "description": "Number of independent observations in the belief graph backing this posterior (effective sample size)."
            },
            "updated_at": {
              "type": "string",
              "format": "date-time",
              "description": "Engine-stamped time of the last posterior write."
            },
            "gaia_package_ref": {
              "type": "string",
              "format": "uri",
              "description": "Optional URI of the belief-graph package (inside the external project) that produced this posterior."
            }
          },
          "additionalProperties": false
        }
      ]
    },
    "activation_condition": {
      "oneOf": [
        {
          "type": "null"
        },
        {
          "type": "object",
          "required": [
            "kind",
            "description",
            "satisfied"
          ],
          "properties": {
            "kind": {
              "enum": [
                "tool_readiness",
                "data_release",
                "stage_reached",
                "exploratory_computation",
                "other"
              ]
            },
            "description": {
              "type": "string",
              "minLength": 1
            },
            "satisfied": {
              "type": "boolean"
            }
          },
          "additionalProperties": false
        }
      ],
      "description": "Present when lifecycle_state is waiting_activation: the condition under which the idea becomes actionable."
    },
    "grounding_audit": {
      "oneOf": [
        {
          "type": "null"
        },
        {
          "type": "object",
          "required": [
            "status",
            "folklore_risk_score",
            "failures",
            "timestamp"
          ],
          "properties": {
            "status": {
              "enum": [
                "pass",
                "fail",
                "partial"
              ]
            },
            "folklore_risk_score": {
              "type": "number",
              "minimum": 0,
              "maximum": 1
            },
            "failures": {
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "timestamp": {
              "type": "string",
              "format": "date-time"
            }
          },
          "additionalProperties": false
        }
      ]
    },
    "reduction_report": {
      "oneOf": [
        {
          "$ref": "./reduction_report_v1.schema.json"
        },
        {
          "type": "null"
        }
      ]
    },
    "reduction_audit": {
      "oneOf": [
        {
          "$ref": "./reduction_audit_v1.schema.json"
        },
        {
          "type": "null"
        }
      ]
    },
    "created_at": {
      "type": "string",
      "format": "date-time"
    },
    "updated_at": {
      "type": "string",
      "format": "date-time",
      "description": "Timestamp of last mutation (e.g., posterior update, lifecycle change, grounding re-audit)."
    }
  },
  "additionalProperties": false
}
