{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "pairwise_match_v1.schema.json",
  "title": "PairwiseMatch v1",
  "description": "Record of one cross-family pairwise comparison between two idea nodes under pre-committed review criteria. Each panel seat votes with anchored arguments only (literature or computation anchors); unanchored arguments are discarded and counted. The outcome is written back to the external belief graph as an observation.",
  "type": "object",
  "required": [
    "match_id",
    "campaign_id",
    "idea_a_node_id",
    "idea_b_node_id",
    "criteria_commitment",
    "panel",
    "outcome",
    "observation_write",
    "independent_runners"
  ],
  "properties": {
    "match_id": {
      "type": "string",
      "pattern": "^[0123456789abcdefghjkmnpqrstvwxyz]{8}$"
    },
    "campaign_id": {
      "type": "string",
      "pattern": "^[0123456789abcdefghjkmnpqrstvwxyz]{8}$"
    },
    "idea_a_node_id": {
      "type": "string",
      "pattern": "^[0123456789abcdefghjkmnpqrstvwxyz]{8}$"
    },
    "idea_b_node_id": {
      "type": "string",
      "pattern": "^[0123456789abcdefghjkmnpqrstvwxyz]{8}$"
    },
    "criteria_commitment": {
      "type": "object",
      "description": "Review criteria committed before the panel sees the pair, so the criteria cannot be adjusted to fit a preferred outcome.",
      "required": [
        "committed_at",
        "criteria",
        "commitment_hash"
      ],
      "properties": {
        "committed_at": {
          "type": "string",
          "format": "date-time"
        },
        "criteria": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "string",
            "minLength": 1
          }
        },
        "commitment_hash": {
          "type": "string",
          "pattern": "^sha256:[a-f0-9]{64}$"
        }
      },
      "additionalProperties": false
    },
    "panel": {
      "type": "array",
      "description": "One entry per judge vote. A valid match holds at least three votes from DISTINCT model families. JSON Schema cannot express \"families must be distinct\", so minItems: 3 here is only the closest schema-expressible floor; the authoritative check on family diversity and the three-distinct-families rule is the Python validator validate_pairwise_match in skills/idea-pairwise-match/scripts/assemble_match.py.",
      "minItems": 3,
      "items": {
        "type": "object",
        "required": [
          "reviewer_family",
          "model",
          "vote",
          "anchored_arguments",
          "unanchored_arguments_discarded"
        ],
        "properties": {
          "reviewer_family": {
            "enum": [
              "claude",
              "codex",
              "opencode",
              "kimi"
            ]
          },
          "model": {
            "type": "string",
            "minLength": 1
          },
          "vote": {
            "enum": [
              "a",
              "b",
              "tie"
            ]
          },
          "anchored_arguments": {
            "type": "array",
            "items": {
              "type": "object",
              "required": [
                "argument",
                "anchor_type",
                "anchor_ref"
              ],
              "properties": {
                "argument": {
                  "type": "string",
                  "minLength": 1
                },
                "anchor_type": {
                  "enum": [
                    "literature",
                    "computation"
                  ]
                },
                "anchor_ref": {
                  "type": "string",
                  "minLength": 1
                }
              },
              "additionalProperties": false
            }
          },
          "unanchored_arguments_discarded": {
            "type": "integer",
            "minimum": 0,
            "description": "Count of arguments this seat produced that carried no literature/computation anchor and were therefore discarded before voting."
          }
        },
        "additionalProperties": false
      }
    },
    "outcome": {
      "type": "object",
      "required": [
        "winner",
        "vote_margin",
        "decided_at"
      ],
      "properties": {
        "winner": {
          "enum": [
            "a",
            "b",
            "tie"
          ]
        },
        "vote_margin": {
          "type": "number"
        },
        "decided_at": {
          "type": "string",
          "format": "date-time"
        }
      },
      "additionalProperties": false
    },
    "observation_write": {
      "type": "object",
      "description": "Whether the match outcome has been written to the external belief graph as an observation.",
      "required": [
        "written"
      ],
      "properties": {
        "written": {
          "type": "boolean"
        },
        "gaia_package_ref": {
          "type": "string",
          "description": "Optional reference to the belief-graph package that received the observation."
        }
      },
      "additionalProperties": false
    },
    "independent_runners": {
      "type": "boolean",
      "description": "Whether the panel seats resolved to genuinely distinct underlying runner commands. False marks a stub-backed or single-command panel whose votes carry no real cross-family diversity, so the belief layer can weight the observation accordingly. Read from the panel run report at assembly time."
    },
    "statement_binding": {
      "type": "object",
      "description": "Per-side binding of the advocacy statement the judges saw: the arguing node id and the sha256 of the statement content. Written when --materials-dir is supplied at assembly (required in a real match); validated by validate_pairwise_match against the recorded node ids.",
      "required": ["a", "b"],
      "properties": {
        "a": {
          "type": "object",
          "required": ["idea_node_id", "content_sha256"],
          "properties": {
            "idea_node_id": { "type": "string", "pattern": "^[0123456789abcdefghjkmnpqrstvwxyz]{8}$" },
            "content_sha256": { "type": "string", "pattern": "^sha256:[0-9a-f]{64}$" }
          },
          "additionalProperties": false
        },
        "b": {
          "type": "object",
          "required": ["idea_node_id", "content_sha256"],
          "properties": {
            "idea_node_id": { "type": "string", "pattern": "^[0123456789abcdefghjkmnpqrstvwxyz]{8}$" },
            "content_sha256": { "type": "string", "pattern": "^sha256:[0-9a-f]{64}$" }
          },
          "additionalProperties": false
        }
      },
      "additionalProperties": false
    },
    "rationale": {
      "type": "string",
      "description": "Optional operator-supplied reason for a rematch of the same idea pair, recorded so a repeat observation carries its stated new-evidence justification."
    }
  },
  "additionalProperties": false
}
